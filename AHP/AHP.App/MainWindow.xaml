<Window x:Class="AHP.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:AHP.App.Controls"
        xmlns:local="clr-namespace:AHP.App"
        mc:Ignorable="d"
        Title="Analytic Hierarchy Process" Height="700" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>

    <Grid>

        <StackPanel>
            <controls:MatrixGrid Margin="10" Matrix="{Binding MyProperty}" VerticalAlignment="Top"/>
            <controls:MatrixGrid Margin="10" Matrix="{Binding MyProperty}" VerticalAlignment="Top"/>
            <controls:ExpertControl Margin="10" Expert="{Binding Expert}" VerticalAlignment="Top"/>
        </StackPanel>

    </Grid>
</Window>
